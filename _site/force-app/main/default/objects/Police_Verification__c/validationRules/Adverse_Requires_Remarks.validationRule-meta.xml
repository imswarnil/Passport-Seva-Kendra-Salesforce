<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Adverse_Requires_Remarks</fullName>
    <active>true</active>
    <description>An adverse PV blocks the passport from being granted; the finding must be documented for the applicant’s appeal and for the granting officer.</description>
    <errorConditionFormula>AND( ISPICKVAL(Status__c, &quot;Adverse&quot;), ISBLANK(Remarks__c) )</errorConditionFormula>
    <errorDisplayField>Remarks__c</errorDisplayField>
    <errorMessage>Enter Remarks describing the adverse finding before setting the status to Adverse.</errorMessage>
</ValidationRule>
