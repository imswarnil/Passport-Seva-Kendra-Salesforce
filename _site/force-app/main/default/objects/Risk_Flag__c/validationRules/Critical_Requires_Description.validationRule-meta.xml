<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Critical_Requires_Description</fullName>
    <active>true</active>
    <description>A Critical flag escalates the application; the reason cannot be left to the flag type alone.</description>
    <errorConditionFormula>AND( ISPICKVAL(Severity__c, &quot;Critical&quot;), ISBLANK(Description__c) )</errorConditionFormula>
    <errorDisplayField>Description__c</errorDisplayField>
    <errorMessage>Enter a Description explaining the critical risk.</errorMessage>
</ValidationRule>
