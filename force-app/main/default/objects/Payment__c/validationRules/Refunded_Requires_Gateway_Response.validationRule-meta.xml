<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Refunded_Requires_Gateway_Response</fullName>
    <active>true</active>
    <description>A refund moves public money back out; the raw gateway response is the evidence trail.</description>
    <errorConditionFormula>AND( ISPICKVAL(Status__c, &quot;Refunded&quot;), ISBLANK(Gateway_Response__c) )</errorConditionFormula>
    <errorDisplayField>Gateway_Response__c</errorDisplayField>
    <errorMessage>Paste the gateway refund response into Gateway Response before marking the payment Refunded.</errorMessage>
</ValidationRule>
