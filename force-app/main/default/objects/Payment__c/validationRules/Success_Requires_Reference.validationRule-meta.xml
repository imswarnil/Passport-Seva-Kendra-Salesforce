<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Success_Requires_Reference</fullName>
    <active>true</active>
    <description>The reference is the only way to reconcile a successful payment against the gateway settlement file.</description>
    <errorConditionFormula>AND( ISPICKVAL(Status__c, &quot;Success&quot;), ISBLANK(Payment_Reference__c) )</errorConditionFormula>
    <errorDisplayField>Payment_Reference__c</errorDisplayField>
    <errorMessage>Enter the gateway Payment Reference before marking the payment Successful.</errorMessage>
</ValidationRule>
