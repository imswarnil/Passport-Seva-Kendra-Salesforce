<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Rejected_Requires_Rejection_Reason</fullName>
    <active>true</active>
    <description>A rejected document sends the applicant back to re-submit; the reason is what tells them what to fix.</description>
    <errorConditionFormula>AND( ISPICKVAL(Status__c, &quot;Rejected&quot;), ISBLANK(Rejection_Reason__c) )</errorConditionFormula>
    <errorDisplayField>Rejection_Reason__c</errorDisplayField>
    <errorMessage>Enter a Rejection Reason when rejecting a document. The applicant is told this reason.</errorMessage>
</ValidationRule>
