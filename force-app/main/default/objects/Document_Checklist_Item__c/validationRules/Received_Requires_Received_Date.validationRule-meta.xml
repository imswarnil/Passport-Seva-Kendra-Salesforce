<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Received_Requires_Received_Date</fullName>
    <active>true</active>
    <description>Document turnaround SLAs are measured from the received date.</description>
    <errorConditionFormula>AND( ISPICKVAL(Status__c, &quot;Received&quot;), ISBLANK(Received_Date__c) )</errorConditionFormula>
    <errorDisplayField>Received_Date__c</errorDisplayField>
    <errorMessage>Enter the Received Date when marking a document as Received.</errorMessage>
</ValidationRule>
