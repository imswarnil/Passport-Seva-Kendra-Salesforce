<?xml version="1.0" encoding="UTF-8"?>
<!--
  Empty companion package for the destructive deploy.

  The Metadata API requires a package.xml alongside destructiveChanges.xml even
  when nothing is being ADDED. This file is that placeholder, and it must stay
  empty: anything listed here would be deployed, not deleted.

  Usage (see MIGRATION.md §3 step 7):
    sf project deploy start \
      --manifest manifest/destructive-package.xml \
      --post-destructive-changes manifest/destructiveChanges.xml \
      --dry-run --verbose
-->
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <version>67.0</version>
</Package>
